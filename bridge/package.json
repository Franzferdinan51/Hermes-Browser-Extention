{
  "name": "hermes-browser-bridge",
  "version": "0.2.0",
  "private": true,
  "description": "Exposes Hermes Agent as an AG-UI-compatible agent endpoint for the Hermes browser extension.",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "test": "node self-test.mjs"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}